// #ifndef SD_CARD_RW_H
// #define SD_CARD_RW_H

// #include <Arduino.h>

// // Test function: writes data to SD card, reads it back, and verifies
// // period: test interval in milliseconds
// // Returns 1 if test was performed and passed, 0 if failed, -1 if test skipped (too soon)
// int testSDCard(unsigned long period);

// #endif